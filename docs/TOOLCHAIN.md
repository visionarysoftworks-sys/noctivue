# TOOLCHAIN.md — Toolchain & Package Manager

**Status:** "One official X" principles Confirmed; exact command
surfaces and manifest format Proposed.

## 1. Principle: One of Everything

To avoid the ecosystem fragmentation seen in languages with multiple
competing formatters/build tools/package managers, Noctivue ships
**one** official formatter, **one** official package manager, **one**
official LSP, and **one** official lockfile format (ADR-010, Principle 9).

## 2. `noct` CLI (Proposed command surface)

```text
noct create     scaffold a new package
noct add        add a dependency
noct build      compile the current package
noct run        build and execute
noct test       run the test suite
noct fmt        run the official formatter
noct lint       run the official linter
noct doc        generate documentation from doc comments
noct publish    publish to the package registry
```

AI-facing extensions to this surface are specified separately in
AI_TOOLING.md (`--json` flags on diagnostics/ast/etc.), since they are a
cross-cutting concern rather than a single command.

## 3. Package Manager

- **Manifest format:** Open — a TOML-like or Noctivue-native (`.nv`-ish)
  manifest are both under consideration; no decision recorded yet.
- **Lockfile:** one canonical, checked-in lockfile format, generated by
  `noct add`/`noct build`, ensuring reproducible builds.
- **Versioning:** semantic versioning (SemVer) for published packages.
- **Resolution:** standard SemVer-range dependency resolution;
  algorithm choice (e.g., PubGrub-style vs. simpler backtracking) is
  Deferred to implementation.
- **Integrity & signing:** package integrity hashes are required before
  any public registry launch; package **signing** is required before
  public registry launch (not merely "eventually") per the design
  brief — this is a security-relevant precondition on the registry
  going live, not a nice-to-have.
- **Dependency trust tier (M7, ADR-015):** every dependency entry
  declares one of `native` / `c-shim` / `cxx-shim` / `wasm-component` /
  `foreign-runtime`. This is required, not inferred, so `noct audit`
  and the registry UI can surface it without re-deriving it — a
  dependency's tier is a fact about how it was integrated, not
  something to guess from its source. `foreign-runtime` dependencies
  are excluded from the default signing/audit trust granted to the
  other four tiers and must be individually opted into; they are not
  eligible to be pulled in transitively without the top-level project
  also declaring that opt-in.

## 4. Formatter

`noct fmt` is normative for canonical style: it **MUST** be able to
losslessly transform between the density levels and block forms defined
in SYNTAX.md §§3, 6–7. Formatter behavior around optional semicolons is
specified in DECISIONS.md Issue 5 (formatter owns semicolon placement).

## 5. Linter

Default-on rules enforce things the grammar can't (unused imports,
unreachable match arms beyond exhaustiveness, etc.). Style-preference
rules — e.g., "prefer concise over explicit declaration style" — ship
**default-off**, per DECISIONS.md Issue 7 and STYLE_GUIDE.md §2, so
teams can opt in without the core language or default tooling mandating
a house style.

## 6. LSP

One official Language Server Protocol implementation backing editor
integrations (see STYLE_GUIDE.md §6 for the file-identity/branding
requirements editors should also honor: icon, syntax highlighting,
language mode, etc.).

## 7. Security Posture

Sophisticated capability-oriented sandboxing is explicitly **Deferred**
past MVP (design brief §28) — language, compiler, runtime, package
system, and FFI are established first; capability security matures once
the ecosystem exists to secure.
