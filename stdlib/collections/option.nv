//! collections/option — compatibility location for Option helpers.
//!
//! The canonical module is `stdlib/option/option.nv`. This file is
//! intentionally header-only so concatenating both paths cannot create
//! duplicate declarations. New code should use the canonical module.
