//! collections/stack — LIFO stack (no code yet).
//!
//! Status: reserved (M4+). Unblocks when: a list-push primitive lands
//! (push cannot be written in pure .nv today, SPEC.md C5).
//! Zero items by design (SPEC.md §6).
