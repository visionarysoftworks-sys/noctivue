//! strings/builder — amortized string builder (no code yet).
//!
//! Status: reserved (M4+). Unblocks when: a string-push primitive lands
//! (repeated concat is O(n^2); a builder without push is dishonest,
//! SPEC.md C5). Zero items by design (SPEC.md §6).
